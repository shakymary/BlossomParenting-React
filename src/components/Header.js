import React from "react";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <header className="container">
      <img src="" />
      <NavBar />
    </header>
  );
};

export default Header;
